specVersion: "v1"
serviceId: "receipt-generator-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "receipt-generator-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
